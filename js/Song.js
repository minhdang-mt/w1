class Song {
    constructor(id, title, artist, duration, url, image) {
        this.id = id;
        this.title = title;
        this.artist = artist;
        this.duration = duration;
        this.url = url;
        this.image = image; // Thêm thuộc tính hình ảnh
    }
}

export default Song;